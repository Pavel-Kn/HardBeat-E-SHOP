import React from "react";
import ProductsList from "../productsList";

const ProductsPage = () => {
    return (
        <div>
            <ProductsList />
        </div>
    );
};

export default ProductsPage;
